server.port=8010
mybatis.mapper-locations=classpath:mapping/*.xml

# 配置数据库连接等数据源
spring.datasource.name=dianping
spring.datasource.url=jdbc:mysql://localhost:3306/dianping?useUnicode=true&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=root

# 使用Druid做连接池管理
spring.datasource.type=com.alibaba.druid.pool.DruidDataSource
spring.datasource.driver-class-name=com.mysql.jdbc.Driver

# 异常处理
# add-mappings=true 表示如果所有的controller都没有被命中，则使用默认的静态资源处理器做匹配
spring.resources.add-mappings=true
spring.mvc.throw-exception-if-no-handler-found=true

# 接入前端静态资源页面
spring.mvc.static-path-pattern=/static/**

# thymeleaf 相关配置
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# admin 官方账号的用户名和密码
admin.email=admin@javaproject.com
admin.encryptPassword=4QrcOUm6Wau+VuBX8g+IPg==